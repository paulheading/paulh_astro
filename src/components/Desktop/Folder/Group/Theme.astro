---
import GroupFolder from "~Folder/Group";
import { get } from "paully/helpers";

const { group } = Astro.props;

const { label } = group[0];

const width = get.rems(124);

const folderProps = {
  width,
  group,
  label,
};
---

<GroupFolder {...folderProps} class="position" />

<style lang="scss">
  @use "~mixins" as *;

  .position {
    @include position-folder;

    @include medium-up {
      @include desktop-grid(3, 4);
    }
  }
</style>
