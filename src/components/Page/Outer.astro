<div class="page-outer">
  <slot />
</div>

<style lang="scss">
  @import "~styles/mixins";

  .page-outer {
    padding: ($padding-row * 1.5) $padding-row;
    font-size: $h5-font-size;

    @include small-up {
      padding-bottom: 9rem;
      font-size: 1.375rem;
    }
  }
</style>
