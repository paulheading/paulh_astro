<div class="page-outer">
  <slot />
</div>

<style lang="scss">
  @use "~mixins" as *;

  .page-outer {
    padding: ($padding-row * 1.5) $padding-row;

    @include small-up {
      padding-bottom: 9rem;
      font-size: toRem(21);
    }
  }
</style>
