---
import Layout from "~layouts/Main.astro";
import Hero from "~components/Hero.astro";

import { data } from "paully";

// import { pocketcasts } from "~data/layout.json";

const layoutProps = {
  keywords: data.seo.keywords("portfolio"),
  description: "I'm a " + data.seo.job.title + ". " + data.seo.job.description,
};
---

<Layout {...layoutProps}>
  <Hero />
</Layout>
